# =============================================================================
# requirements.txt — Python Dependencies
# =============================================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# What each package does:
# =============================================================================

# Anthropic Python SDK — for calling Claude API (the LLM generator)
anthropic>=0.40.0

# Sentence Transformers — for creating text embeddings locally (no API needed)
# Downloads the embedding model (~80MB) automatically on first use
sentence-transformers>=3.0.0

# NumPy — for vector math (cosine similarity, array operations)
numpy>=1.24.0

# torch — required by sentence-transformers (neural network backend)
# IMPORTANT: Install the CPU-only build — do NOT just run `pip install torch`
# The default torch includes CUDA (GPU) libraries that fail on machines without NVIDIA GPU.
# CPU-only install command:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# pytest — the test framework we use for all tests in tests/
# Run tests with:  pytest tests/ -v
pytest>=7.0.0

# openai — only needed when LLM_PROVIDER = "ollama" in config.py
# Ollama exposes an OpenAI-compatible API, so we reuse this client
# pointed at localhost instead of api.openai.com
# Install when using Ollama: pip install openai
# openai>=1.0.0
