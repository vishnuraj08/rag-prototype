# This file makes the 'components' directory a Python package.
# It allows imports like: from components.loader import DocumentLoader
# Without this file, Python wouldn't recognize 'components' as importable.
